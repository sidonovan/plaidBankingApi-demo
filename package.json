{
  "name": "bank-dashboard-master",
  "version": "1.0.0",
  "description": "A banking dashboard web app built with Node.js and Express.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "del-cli dist \"public/**/*.js\" \"public/**/*.js.map\"",
    "build:server": "tsc -p tsconfig.json",
    "build:client": "tsc -p tsconfig.frontend.json",
    "build": "npm run clean && npm run build:server && npm run build:client",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "plaid": "^39.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^20.11.24",
    "del-cli": "^7.0.0",
    "rimraf": "^6.1.0",
    "typescript": "^5.4.5",
    "undici-types": "^7.16.0"
  }
}
